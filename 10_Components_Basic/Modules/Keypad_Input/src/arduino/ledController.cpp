#include "ledController.h"

LedController::LedController() {}

void LedController::begin()
{
    // TODO: configure LED pins
}

void LedController::setSuccess(bool on) { (void)on; }
void LedController::setFailure(bool on) { (void)on; }
void LedController::setAdminMode(bool on) { (void)on; }
