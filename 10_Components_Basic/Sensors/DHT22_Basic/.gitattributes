# Global line ending and diff settings
# Normalize all text files to LF in the repository; Git will checkout with LF unless overridden.
* text=auto eol=lf

# Windows batch scripts should keep CRLF so they run correctly in cmd / PowerShell
*.bat text eol=crlf
*.cmd text eol=crlf

# Explicit LF endings for source & config
*.py text eol=lf
*.sh text eol=lf
*.json text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.md text eol=lf
*.toml text eol=lf
*.lock text eol=lf

# Binary files (no text normalization)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.gz binary
*.bz2 binary
*.7z binary
*.pdf binary
*.exe binary
*.dll binary

# Logs - do not normalize (treat as text but keep endings stable)
logs/** -text
