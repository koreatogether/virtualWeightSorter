02버전의 기존 로직들은 그대로 둔다.

다만 다음의 컴포넌트들은 다음과 같이 수정한다.

## t1 컴포넌트
2026-01-31 22:30:05초로 시작해서 
1초마다 시간을 더한다.

## dualStateButton 컴포넌트들

bt0 는 토글이 되었으면 1채널 만 남기고 나머지 채널은 숨긴다. (끈다 )
bt1 는 토글이 되었으면 2채널 만 남기고 나머지 채널은 숨긴다. (끈다 )
bt2 는 토글이 되었으면 3채널 만 남기고 나머지 채널은 숨긴다. (끈다 )
bt3 는 토글이 되었으면 4채널 만 남기고 나머지 채널은 숨긴다. (끈다 )
bt4 는 토글이 되었으면 모든 채널을 다 활성화 시킨다.

5가지 dualStateButton둘중 어떤것도 토글이 안되었다면
웨이브폼에서 채널들은 다 꺼짐 상태이다.

## button 컴포넌트들
b0는 page0를 뜻한다.
b1는 page1를 뜻한다.
b2는 page2를 뜻한다.
b3는 page3를 뜻한다.

각 버튼의 눌림이 감지되면 각 해당 페이지로 이동한다.
다만 되돌아오는 버튼은 아직 구현 안됨 
