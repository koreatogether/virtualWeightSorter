# TODO 01 — 버퍼에서 데이터 읽어와 디스플레이에 출력

설명:
- 버퍼에 저장된 데이터(예: waveformBuffer)에서 데이터를 읽어 Nextion 디스플레이에 출력하는 로직이 누락되어 있습니다.

요구 사항:
- 버퍼에서 최신 샘플을 읽어 파형에 기록하고, 동기화 로직(채널 동기화)이 제대로 작동하도록 구현.
- 낮은 지연과 안정성을 위해 전송구간에 적절한 딜레이/배치 처리 적용.

검증(테스트 케이스):
- 시뮬레이트된 센서 데이터를 넣고 Nextion에서 정상적으로 그래프가 갱신되는지 확인.
- 여러 채널 on/off 시 동기화(채널 캡처) 테스트.

실 테스트 결과 :

- Nextion 디스플레이에서 채널 on/off 버튼을 눌러도  버퍼에 기록된 그대로  그래프가 정상적으로 그려지고 있음 
상태:
- 담당자: Copilot
- 시작일: 2026-02-06
- 완료일: 2026-02-06
- 진행상태: done

첨부(로그/스크린샷):

메모:
- `NextionManager.cpp`의 `storeWaveformSamples`, `syncChannelToCurrentPosition`, `updateNextionDisplay` 부분과 연계하여 구현 필요.